workflows:
  ios_release_manual:
    name: iOS Release (Wonder Space Gallery)
    instance_type: mac_mini_m2
    max_build_duration: 120

    environment:
      flutter: stable
      xcode: latest
      groups:
        - ios_credentials
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.ctmd.wonderforge.wonderspace.gallery
      vars:
        TEAM_ID: "CS2UV6F3ZY"
        BUNDLE_ID: "com.ctmd.wonderforge.wonderspace.gallery"
        CERT_NAME: "Apple Distribution: Cuong Nguyen Ba (CS2UV6F3ZY)"
        PROFILE_NAME: "WonderSpaceGallery_AppStore_Profile"

    scripts:
      - name: 🧹 Clean & Install Pods
        script: |
          set -e
          echo "🧹 Cleaning Flutter build..."
          flutter clean
          flutter pub get
          flutter precache --ios
          cd ios
          pod repo update
          pod install
          cd ..
          echo "✅ CocoaPods installed successfully."

      - name: 🔨 Build Flutter iOS (no codesign)
        script: |
          set -e
          echo "🔨 Detecting Flutter.framework..."
          mkdir -p ios/Flutter

          # Detect Flutter SDK path safely (Codemagic M1/M2, macOS)
          if command -v greadlink &>/dev/null; then
            FLUTTER_BIN_PATH=$(greadlink -f "$(which flutter)")
          else
            FLUTTER_BIN_PATH=$(readlink "$(which flutter)" || which flutter)
          fi

          # Resolve deeper symlink if needed
          if [[ ! "$FLUTTER_BIN_PATH" == */flutter ]]; then
            FLUTTER_BIN_PATH=$(ls -l "$(which flutter)" | awk '{print $NF}')
          fi

          FLUTTER_SDK_ROOT=$(dirname $(dirname "$FLUTTER_BIN_PATH"))

          echo "📍 Flutter SDK root detected at: $FLUTTER_SDK_ROOT"

          # Try to find Flutter.framework
          FLUTTER_FRAMEWORK_PATH=$(find "${FLUTTER_SDK_ROOT}/bin/cache/artifacts/engine" -name "Flutter.framework" -type d 2>/dev/null | head -n 1)

          if [ -z "$FLUTTER_FRAMEWORK_PATH" ]; then
            echo "⚠️ Flutter.framework not found, running flutter precache..."
            flutter precache --ios
            FLUTTER_FRAMEWORK_PATH=$(find "${FLUTTER_SDK_ROOT}/bin/cache/artifacts/engine" -name "Flutter.framework" -type d 2>/dev/null | head -n 1)
          fi

          if [ -z "$FLUTTER_FRAMEWORK_PATH" ]; then
            echo "❌ Could not locate Flutter.framework after precache!"
            echo "🔍 Dumping Flutter engine contents:"
            ls -R "${FLUTTER_SDK_ROOT}/bin/cache/artifacts/engine" || true
            exit 1
          else
            echo "✅ Found Flutter.framework at: $FLUTTER_FRAMEWORK_PATH"
            cp -R "$FLUTTER_FRAMEWORK_PATH" ios/Flutter/
          fi

          echo "📦 Building Flutter iOS app (no codesign)..."
          flutter build ios --release --no-codesign
          echo "✅ Flutter iOS build completed successfully."

      - name: 🚀 Build iOS Archive (App Store)
        script: |
          set -e
          echo "🚀 Running Xcode archive..."
          mkdir -p build/ios/archive
          xcodebuild archive \
            -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -archivePath build/ios/archive/Runner.xcarchive \
            -destination 'generic/platform=iOS' \
            CODE_SIGN_STYLE=Manual \
            DEVELOPMENT_TEAM="${TEAM_ID}" \
            CODE_SIGN_IDENTITY="${CERT_NAME}" \
            PROVISIONING_PROFILE_SPECIFIER="${PROFILE_NAME}" \
            CODE_SIGNING_ALLOWED=YES \
            CODE_SIGNING_REQUIRED=YES

      - name: 📦 Export signed IPA (App Store)
        script: |
          set -e
          echo "📦 Exporting signed IPA using exportOptions_appstore.plist..."
          xcodebuild -exportArchive \
            -archivePath build/ios/archive/Runner.xcarchive \
            -exportOptionsPlist ios/exportOptions_appstore.plist \
            -exportPath build/ios/ipa
          echo "✅ IPA exported successfully."

      - name: 📋 Show IPA files
        script: |
          echo "📂 IPA files:"
          find build/ios/ipa -type f -name "*.ipa"

    artifacts:
      - build/ios/ipa/*.ipa

  ios_adhoc_manual:
    name: iOS AdHoc (Wonder Space Gallery)
    instance_type: mac_mini_m2
    max_build_duration: 120

    environment:
      flutter: stable
      xcode: latest
      groups:
        - ios_credentials
      ios_signing:
        distribution_type: ad_hoc
        bundle_identifier: com.ctmd.wonderforge.wonderspace.gallery
      vars:
        TEAM_ID: "CS2UV6F3ZY"
        BUNDLE_ID: "com.ctmd.wonderforge.wonderspace.gallery"
        CERT_NAME: "Apple Distribution: Cuong Nguyen Ba (CS2UV6F3ZY)"
        PROFILE_NAME: "wonderspace_gallery_adhoc"

    scripts:
      - name: 🧹 Clean & Install Pods
        script: |
          set -e
          flutter clean
          flutter pub get
          flutter precache --ios
          cd ios
          pod install
          cd ..

      - name: 🔨 Build Flutter iOS (no codesign)
        script: |
          set -e
          echo "🔨 Detecting Flutter.framework..."
          mkdir -p ios/Flutter

          if command -v greadlink &>/dev/null; then
            FLUTTER_BIN_PATH=$(greadlink -f "$(which flutter)")
          else
            FLUTTER_BIN_PATH=$(readlink "$(which flutter)" || which flutter)
          fi

          if [[ ! "$FLUTTER_BIN_PATH" == */flutter ]]; then
            FLUTTER_BIN_PATH=$(ls -l "$(which flutter)" | awk '{print $NF}')
          fi

          FLUTTER_SDK_ROOT=$(dirname $(dirname "$FLUTTER_BIN_PATH"))
          echo "📍 Flutter SDK root detected at: $FLUTTER_SDK_ROOT"

          FLUTTER_FRAMEWORK_PATH=$(find "${FLUTTER_SDK_ROOT}/bin/cache/artifacts/engine" -name "Flutter.framework" -type d 2>/dev/null | head -n 1)
          if [ -z "$FLUTTER_FRAMEWORK_PATH" ]; then
            echo "⚠️ Flutter.framework not found, running flutter precache..."
            flutter precache --ios
            FLUTTER_FRAMEWORK_PATH=$(find "${FLUTTER_SDK_ROOT}/bin/cache/artifacts/engine" -name "Flutter.framework" -type d 2>/dev/null | head -n 1)
          fi

          if [ -z "$FLUTTER_FRAMEWORK_PATH" ]; then
            echo "❌ Could not locate Flutter.framework after precache!"
            echo "🔍 Dumping Flutter engine contents:"
            ls -R "${FLUTTER_SDK_ROOT}/bin/cache/artifacts/engine" || true
            exit 1
          else
            echo "✅ Found Flutter.framework at: $FLUTTER_FRAMEWORK_PATH"
            cp -R "$FLUTTER_FRAMEWORK_PATH" ios/Flutter/
          fi

          echo "📦 Building Flutter iOS app (no codesign)..."
          flutter build ios --release --no-codesign
          echo "✅ Flutter iOS build completed successfully."

      - name: 🚀 Build iOS Archive (Ad Hoc)
        script: |
          set -e
          echo "🚀 Running Xcode archive (Ad Hoc)..."
          mkdir -p build/ios/archive
          xcodebuild archive \
            -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -archivePath build/ios/archive/Runner.xcarchive \
            -destination 'generic/platform=iOS' \
            CODE_SIGN_STYLE=Manual \
            DEVELOPMENT_TEAM="${TEAM_ID}" \
            CODE_SIGN_IDENTITY="${CERT_NAME}" \
            PROVISIONING_PROFILE_SPECIFIER="${PROFILE_NAME}" \
            CODE_SIGNING_ALLOWED=YES \
            CODE_SIGNING_REQUIRED=YES

      - name: 📦 Export signed IPA (Ad Hoc)
        script: |
          set -e
          echo "📦 Exporting signed IPA using exportOptions_adhoc.plist..."
          xcodebuild -exportArchive \
            -archivePath build/ios/archive/Runner.xcarchive \
            -exportOptionsPlist ios/exportOptions_adhoc.plist \
            -exportPath build/ios/ipa
          echo "✅ AdHoc IPA exported successfully."

      - name: 📋 Show IPA Path
        script: |
          echo "📂 IPA ready at:"
          find build/ios/ipa -name "*.ipa"

    artifacts:
      - build/ios/ipa/*.ipa
