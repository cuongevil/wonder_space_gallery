workflows:
  ios_release_manual:
    name: iOS Release (Wonder Space Gallery)
    instance_type: mac_mini_m2
    max_build_duration: 120

    environment:
      flutter: stable
      xcode: latest
      groups:
        - ios_credentials
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.ctmd.wonderforge.wonderspace.gallery
      vars:
        TEAM_ID: "CS2UV6F3ZY"
        BUNDLE_ID: "com.ctmd.wonderforge.wonderspace.gallery"
        CERT_NAME: "Apple Distribution: Cuong Nguyen Ba (CS2UV6F3ZY)"
        PROFILE_NAME: "WonderSpaceGallery_AppStore_Profile"

    scripts:
      - name: 🧹 Clean & Prepare
        script: |
          set -e
          flutter clean
          flutter pub get
          cd ios && pod repo update && pod install && cd ..
          echo "✅ Pods ready."

      - name: 🧾 Create exportOptions.plist
        script: |
          set -e
          mkdir -p build/ios
          cat > build/ios/exportOptions.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store</string>
            <key>teamID</key>
            <string>${TEAM_ID}</string>
            <key>provisioningProfiles</key>
            <dict>
              <key>${BUNDLE_ID}</key>
              <string>${PROFILE_NAME}</string>
            </dict>
            <key>destination</key>
            <string>export</string>
            <key>compileBitcode</key>
            <false/>
          </dict>
          </plist>
          EOF

      - name: 🧩 Ensure Scheme + Archive folder
        script: |
          set -e
          echo "🧩 Ensuring Runner scheme and archive directory..."
          mkdir -p build/ios/archive
          cd ios
          xcodebuild -list -workspace Runner.xcworkspace
          if ! xcodebuild -showBuildSettings -workspace Runner.xcworkspace -scheme Runner >/dev/null 2>&1; then
            echo "⚠️ Scheme Runner not found — regenerating..."
            flutter build ios --release
          fi
          cd ..

      - name: 🚀 Build iOS Archive
        script: |
          set -e
          echo "🚀 Running Xcode archive..."
          xcodebuild archive \
            -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -archivePath build/ios/archive/Runner.xcarchive \
            -destination 'generic/platform=iOS,name=Any iOS Device' \
            CODE_SIGN_STYLE=Manual \
            DEVELOPMENT_TEAM="${TEAM_ID}" \
            CODE_SIGN_IDENTITY="${CERT_NAME}" \
            PROVISIONING_PROFILE_SPECIFIER="${PROFILE_NAME}" \
            CODE_SIGNING_ALLOWED=YES \
            CODE_SIGNING_REQUIRED=YES \
            | tee build_log.txt | xcpretty

          if [ ! -d "build/ios/archive/Runner.xcarchive" ]; then
            echo "⚠️ Archive not found in build/ios/archive — checking fallback..."
            LATEST_ARCHIVE=$(ls -dt ~/Library/Developer/Xcode/Archives/*/*Runner.xcarchive 2>/dev/null | head -n 1)
            if [ -n "$LATEST_ARCHIVE" ]; then
              echo "📦 Found fallback archive: $LATEST_ARCHIVE"
              mkdir -p build/ios/archive
              cp -R "$LATEST_ARCHIVE" build/ios/archive/Runner.xcarchive
            else
              echo "❌ No archive found anywhere!"
              exit 65
            fi
          fi
          echo "✅ Archive ready at build/ios/archive/Runner.xcarchive"

      - name: 📦 Export signed IPA
        script: |
          set -e
          echo "📦 Exporting signed IPA..."
          xcodebuild -exportArchive \
            -archivePath build/ios/archive/Runner.xcarchive \
            -exportOptionsPlist build/ios/exportOptions.plist \
            -exportPath build/ios/ipa
          echo "✅ IPA exported successfully."

      - name: 📋 Show IPA
        script: |
          echo "📂 IPA files:"
          find build/ios/ipa -type f -name "*.ipa"

    artifacts:
      - build/ios/ipa/*.ipa
