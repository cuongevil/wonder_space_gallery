workflows:
  ios_release_manual:
    name: iOS Release (Wonder Space Gallery)
    instance_type: mac_mini_m2
    max_build_duration: 120

    environment:
      flutter: stable
      xcode: latest
      groups:
        - ios_credentials
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.ctmd.wonderforge.wonderspace.gallery
      vars:
        TEAM_ID: "CS2UV6F3ZY"
        BUNDLE_ID: "com.ctmd.wonderforge.wonderspace.gallery"
        CERT_NAME: "Apple Distribution: Cuong Nguyen Ba (CS2UV6F3ZY)"
        PROFILE_NAME: "WonderSpaceGallery_AppStore_Profile"

    scripts:
      - name: 🧹 Clean & Prepare
        script: |
          set -e
          echo "🧹 Cleaning Flutter and preparing Pods..."
          flutter clean
          flutter pub get
          cd ios
          pod deintegrate
          pod repo update
          pod install --repo-update
          cd ..
          echo "✅ Environment ready."

      - name: 🧾 Create exportOptions.plist
        script: |
          set -e
          mkdir -p build/ios
          cat > build/ios/exportOptions.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store</string>
            <key>teamID</key>
            <string>${TEAM_ID}</string>
            <key>provisioningProfiles</key>
            <dict>
              <key>${BUNDLE_ID}</key>
              <string>${PROFILE_NAME}</string>
            </dict>
            <key>compileBitcode</key>
            <false/>
            <key>destination</key>
            <string>export</string>
          </dict>
          </plist>
          EOF
          echo "✅ exportOptions.plist created."

      - name: 🚀 Build iOS Release (Archive)
        script: |
          set -e
          echo "🚀 Archiving iOS app via Xcode..."
          mkdir -p build/ios/archive

          # Clean project first
          xcodebuild clean -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release | xcpretty

          # Archive the app (explicit destination for iOS)
          xcodebuild archive -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -archivePath build/ios/archive/Runner.xcarchive \
            -destination "generic/platform=iOS" \
            DEVELOPMENT_TEAM="${TEAM_ID}" \
            CODE_SIGN_IDENTITY="${CERT_NAME}" \
            PROVISIONING_PROFILE_SPECIFIER="${PROFILE_NAME}" \
            CODE_SIGNING_ALLOWED=YES \
            CODE_SIGNING_REQUIRED=YES | tee build_log.txt | xcpretty

          # Verify archive created successfully
          if [ ! -d "build/ios/archive/Runner.xcarchive" ]; then
            echo "❌ Archive failed! Showing last 50 lines of Xcode log:"
            tail -n 50 build_log.txt
            exit 1
          fi
          echo "✅ Xcode archive created at build/ios/archive/Runner.xcarchive"

      - name: 🧩 Verify Archive
        script: |
          set -e
          echo "🔍 Verifying that the archive exists..."
          if [ ! -d "build/ios/archive/Runner.xcarchive" ]; then
            echo "❌ Archive missing! Build failed before export."
            exit 1
          fi
          echo "✅ Archive found. Proceeding to export."

      - name: 🔧 Fix CFBundleIdentifiers in Frameworks
        script: |
          set -e
          echo "🔧 Checking and fixing framework Info.plist identifiers..."
          APP_PATH="build/ios/archive/Runner.xcarchive/Products/Applications/Runner.app"
          find "$APP_PATH/Frameworks" -name Info.plist | while read plist; do
            CURRENT_ID=$(/usr/libexec/PlistBuddy -c "Print CFBundleIdentifier" "$plist" 2>/dev/null || true)
            FRAMEWORK_NAME=$(basename "$(dirname "$plist")" .framework)
            if [ -z "$CURRENT_ID" ]; then
              FIXED_ID="${BUNDLE_ID}.${FRAMEWORK_NAME}"
              echo "⚙️ Added CFBundleIdentifier: $FIXED_ID"
              /usr/libexec/PlistBuddy -c "Add :CFBundleIdentifier string $FIXED_ID" "$plist" || \
              /usr/libexec/PlistBuddy -c "Set :CFBundleIdentifier $FIXED_ID" "$plist" || true
            fi
            if [[ "$CURRENT_ID" == *"_"* ]]; then
              FIXED_ID=$(echo "$CURRENT_ID" | sed 's/_/-/g')
              echo "🔄 Sanitized: $CURRENT_ID → $FIXED_ID"
              /usr/libexec/PlistBuddy -c "Set :CFBundleIdentifier $FIXED_ID" "$plist" || true
            fi
          done
          echo "✅ All framework Info.plist files sanitized."

      - name: 📦 Export IPA (Final)
        script: |
          set -e
          echo "📦 Exporting signed IPA..."
          mkdir -p build/ios/ipa
          xcodebuild -exportArchive \
            -archivePath build/ios/archive/Runner.xcarchive \
            -exportOptionsPlist build/ios/exportOptions.plist \
            -exportPath build/ios/ipa \
            CODE_SIGNING_ALLOWED=YES \
            CODE_SIGNING_REQUIRED=YES | xcpretty
          echo "✅ IPA exported successfully."

      - name: 🧾 Verify IPA before upload
        script: |
          set -e
          echo "🔍 Verifying generated IPA contents..."
          IPA_PATH=$(find build/ios/ipa -name "*.ipa" | head -n 1)
          if [ -z "$IPA_PATH" ]; then
            echo "❌ IPA not found. Something went wrong during export."
            exit 1
          fi
          unzip -q "$IPA_PATH" -d ipa_check
          echo "✅ IPA found and unzipped successfully."
          echo "Checking Info.plist files for valid CFBundleIdentifiers..."
          find ipa_check -name Info.plist | while read plist; do
            ID=$(/usr/libexec/PlistBuddy -c "Print CFBundleIdentifier" "$plist" 2>/dev/null || true)
            if [ -z "$ID" ]; then
              echo "❌ Missing CFBundleIdentifier in $plist"
              exit 1
            fi
          done
          echo "✅ All CFBundleIdentifiers are valid."
          rm -rf ipa_check

      - name: 📋 Show IPA
        script: |
          echo "📂 Final IPA file(s):"
          find build/ios/ipa -type f -name "*.ipa"

    artifacts:
      - build/ios/ipa/*.ipa
